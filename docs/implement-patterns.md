aqui me gustaria colocar cosas como guia de como se commitea, actualmente todos los commits se hacen en develop y esto hace que no quede tan bonito, podriamos hacer que definimos ramas generale segun el tipo de caso de la tarea, pero no generar una rama por cada tarea, ademas respetar la forma del commit, creo que esto lo tengo en contributing si esnecesario modificar eso pues para que este consistente hazlo, ademas, el agente no commitea si es que no se le avisa ya que puede commitear algo que no es funcional, por lo que debe preguntar antes, los pr son a push o no sé como es la logica actual pero sigue el juego, logica de que push en main es deploy en vercel, que cada vez que se habla de api key hay que corroborar que haya consistencia entre los .env, tambien si se va a crear un componente es necesario que no se haga todo en un archivo sino que sea refactorizado segun como está los componentes vecinos actualmente siguen una buena estructura (no se como se llama), tambien que se mantenga consistente el schema prisma con la bd. 
TODO ESTO DEBES REDACTARLO MEJOR, ESTRUCTURARLO MEJOR, DARME IDEAS DE QUE MAS SE PUEDE AÑADIR AQUI Y DARME UN RESUMEN PARA NO TENER QUE LEER TODO DE LO QUE SE REDACTA